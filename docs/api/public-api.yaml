# generated by tools/docsgen; do not edit. Re-run: make docs-gen
openapi: 3.0.3
info:
  title: Authplane Authserver — Public API
  version: 0.1.0
  description: |-
    Generated from the authserver handler source by `tools/docsgen`. The human-readable companion reference is `docs/reference/http-api.md`; this YAML is its OpenAPI 3.0.3 projection for tooling consumers (Swagger UI, openapi-validator, client codegen, contract testing). Run `make docs-gen` to regenerate.

    The public server (default `:9000`) hosts the OAuth 2.1 / OIDC / MCP endpoints clients and agents interact with. Most endpoints are unauthenticated at the transport layer; per-grant authentication is in the request body (`client_id`/`client_secret`, `subject_token`, DPoP proofs, …).
servers:
  - url: http://localhost:9000
    description: local development (default port)
tags:
  - name: .well-known
  - name: connect
  - name: connections
  - name: consent
  - name: health
  - name: livez
  - name: login
  - name: logout
  - name: oauth
  - name: oidc
  - name: ready
paths:
  /.well-known/jwks.json:
    get:
      operationId: http-public-well-known-jwks-json
      summary: '`GET /.well-known/jwks.json`'
      tags:
        - .well-known
      responses:
        "200":
          description: OK
  /.well-known/oauth-authorization-server:
    get:
      operationId: http-public-well-known-oauth-authorization-server
      summary: '`GET /.well-known/oauth-authorization-server`'
      tags:
        - .well-known
      responses:
        "200":
          description: OK
  /.well-known/oauth-protected-resource:
    get:
      operationId: http-public-well-known-oauth-protected-resource
      summary: '`GET /.well-known/oauth-protected-resource`'
      tags:
        - .well-known
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
  /.well-known/oauth-protected-resource/{ref...}:
    get:
      operationId: http-public-well-known-oauth-protected-resource-ref
      summary: '`GET /.well-known/oauth-protected-resource/{ref...}`'
      tags:
        - .well-known
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
    parameters:
      - name: ref...
        in: path
        required: true
        schema:
          type: string
  /.well-known/openid-configuration:
    get:
      operationId: http-public-well-known-openid-configuration
      summary: '`GET /.well-known/openid-configuration`'
      tags:
        - .well-known
      responses:
        "200":
          description: OK
  /connect/{provider}:
    get:
      operationId: http-public-connect-provider
      summary: '`GET /connect/{provider}`'
      tags:
        - connect
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
      security:
        - SessionCookie: []
    parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
  /connect/{provider}/callback:
    get:
      operationId: http-public-connect-provider-callback
      summary: '`GET /connect/{provider}/callback`'
      tags:
        - connect
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
      security:
        - SessionCookie: []
    parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
  /connections:
    get:
      operationId: http-public-connections
      summary: '`GET /connections`'
      tags:
        - connections
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
      security:
        - SessionCookie: []
  /connections/{provider}:
    delete:
      operationId: http-public-connections-provider-delete
      summary: '`DELETE /connections/{provider}`'
      tags:
        - connections
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
      security:
        - SessionCookie: []
    parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
  /consent:
    get:
      operationId: http-public-consent
      summary: '`GET /consent`'
      tags:
        - consent
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
      security:
        - SessionCookie: []
    post:
      operationId: http-public-consent-post
      summary: '`POST /consent`'
      tags:
        - consent
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
      security:
        - SessionCookie: []
  /health:
    get:
      operationId: http-public-health
      summary: '`GET /health`'
      tags:
        - health
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /livez:
    get:
      operationId: http-public-livez
      summary: '`GET /livez`'
      tags:
        - livez
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /login:
    get:
      operationId: http-public-login
      summary: '`GET /login`'
      tags:
        - login
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
    post:
      operationId: http-public-login-post
      summary: '`POST /login`'
      tags:
        - login
      responses:
        "303":
          description: Status 303
        "404":
          description: Not Found
        "422":
          description: Status 422
        "429":
          description: Too Many Requests
  /logout:
    post:
      operationId: http-public-logout-post
      summary: '`POST /logout`'
      tags:
        - logout
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
  /oauth/authorize:
    get:
      operationId: http-public-oauth-authorize
      summary: '`GET /oauth/authorize`'
      tags:
        - oauth
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
      security:
        - SessionCookie: []
  /oauth/introspect:
    post:
      operationId: http-public-oauth-introspect
      summary: '`POST /oauth/introspect`'
      tags:
        - oauth
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              description: Form-encoded grant parameters; concrete fields depend on `grant_type`. See `docs/reference/http-api.md` for the per-grant breakdown.
              additionalProperties: true
      responses:
        "200":
          description: OK
  /oauth/register:
    post:
      operationId: http-public-oauth-register
      summary: '`POST /oauth/register`'
      description: This endpoint creates **user-delegated clients**. Their scopes come from the user at consent time, so a `scope` member in the request is **discarded** and the response carries none. **Register machine-to-machine clients (`client_credentials`, jwt-bearer) with `POST /admin/clients` instead** — that is the only surface that sets a client's scope ceiling, so a client registered here starts without one and those two grants refuse every explicit `scope` at `POST /oauth/token` with `invalid_scope`.
      tags:
        - oauth
      responses:
        "201":
          description: Created
  /oauth/revoke:
    post:
      operationId: http-public-oauth-revoke
      summary: '`POST /oauth/revoke`'
      tags:
        - oauth
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              description: Form-encoded grant parameters; concrete fields depend on `grant_type`. See `docs/reference/http-api.md` for the per-grant breakdown.
              additionalProperties: true
      responses:
        "200":
          description: OK
  /oauth/token:
    post:
      operationId: http-public-oauth-token
      summary: '`POST /oauth/token`'
      description: 'Issues an access token for the grant named in `grant_type` (RFC 6749 §3.2): `authorization_code`, `refresh_token`, `client_credentials`, token-exchange and jwt-bearer. The request is form-encoded; DPoP-bound clients send a `DPoP` header. `invalid_scope` on `client_credentials` or jwt-bearer usually means an empty client ceiling: the client''s registered `scope` is read only by those two grants, and set only by the admin surface (`POST /admin/clients`, `PATCH /admin/clients/{client_id}`).'
      tags:
        - oauth
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              description: Form-encoded grant parameters; concrete fields depend on `grant_type`. See `docs/reference/http-api.md` for the per-grant breakdown.
              additionalProperties: true
      responses:
        "200":
          description: 'OK (Alternate shape: `tokenExchangeResponseDTO` — see the human-readable reference for the discriminator.)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponseDTO'
        "400":
          description: 'Bad Request (`error` values: `invalid_request`, `invalid_client`, `invalid_grant`, `unauthorized_client`, `unsupported_grant_type`, `invalid_scope`, `consent_required`, `consent_url`)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthErrorResponse'
  /oidc/callback:
    get:
      operationId: http-public-oidc-callback
      summary: '`GET /oidc/callback`'
      tags:
        - oidc
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
  /oidc/start:
    get:
      operationId: http-public-oidc-start
      summary: '`GET /oidc/start`'
      tags:
        - oidc
      responses:
        default:
          description: See `docs/reference/http-api.md` for the response shape.
  /ready:
    get:
      operationId: http-public-ready
      summary: '`GET /ready`'
      tags:
        - ready
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
components:
  schemas:
    HealthResponse:
      type: object
      properties:
        db:
          type: string
        status:
          type: string
        time:
          type: string
      required:
        - status
        - time
      description: healthResponse is the JSON body for GET /livez, GET /health and GET /ready.
    OAuthErrorResponse:
      type: object
      properties:
        cause:
          type: string
          description: 'Cause is a sub-discriminator for consent_required errors. Values currently in use: "consent_missing" (user never authorized this agent for this resource), "scope_insufficient" (user authorized a strict subset of the requested scopes). Empty for non-consent errors and for legacy consent errors that predate the field. Wire format is `cause` (lowercase, omitempty).'
        consent_url:
          type: string
          description: ConsentURL is a URL to a consent page where the user can authorize the AS to access an upstream provider. Populated only by WriteOAuthErrorWithConsent / WriteOAuthErrorWithConsentAndCause; omitted from all other error responses.
        detail:
          type: string
        error:
          type: string
        error_description:
          type: string
        status:
          type: integer
          format: int32
        title:
          type: string
        type:
          type: string
      required:
        - detail
        - error
        - error_description
        - status
        - title
        - type
      description: OAuthErrorResponse is the combined OAuth + RFC 9457 error response.
    TokenExchangeResponseDTO:
      type: object
      properties:
        access_token:
          type: string
        expires_in:
          type: integer
          format: int32
        issued_token_type:
          type: string
        scope:
          type: string
        token_type:
          type: string
      required:
        - access_token
        - expires_in
        - issued_token_type
        - token_type
      description: tokenExchangeResponseDTO is the JSON response for RFC 8693 token exchange. It includes issued_token_type which is not present in standard token responses.
    TokenResponseDTO:
      type: object
      properties:
        access_token:
          type: string
        expires_in:
          type: integer
          format: int32
        refresh_token:
          type: string
        scope:
          type: string
        token_type:
          type: string
      required:
        - access_token
        - expires_in
        - token_type
      description: tokenResponseDTO is the JSON structure for POST /oauth/token responses.
  securitySchemes:
    OAuth2Bearer:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: OAuth 2.1 access token issued by `/oauth/token` (or via federation through `/oidc/start` -> `/oidc/callback`).
    SessionCookie:
      type: apiKey
      name: authplane_session
      in: cookie
      description: Browser session cookie set by the AS login + consent flows. Used by the human-driven endpoints (`/oauth/authorize`, `/connect/{provider}`, …).
